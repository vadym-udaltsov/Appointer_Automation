#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('stage one') {
            steps {
                echo "after first step in stage on ------"
            }
        }
        stage('stage two') {
            steps {
                echo "stage two: -------"
            }
        }
        stage('stage three') {
            steps {
                echo "stage three -----"
            }
        }
    }
}